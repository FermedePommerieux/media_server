# Configuration DVD Archiver - Phase 2 (Scan MKV + IA)
# Copier vers /etc/dvdarchiver.conf puis adapter les chemins.

# --- Phase 1 (rappel) ---
DEST="/mnt/media_master"

# --- Phase 2: Scan ONLY-MKV ---
SCAN_QUEUE_DIR="/var/spool/dvdarchiver-scan"
SCAN_LOG_DIR="/var/log/dvdarchiver-scan"

# Détection d'archives prêtes (présence d'au moins un .mkv)
SCAN_TRIGGER_GLOB="${DEST:-/mnt/media_master}/*/mkv/*.mkv"

# Extraction métadonnées MKV
MKVMERGE_BIN="mkvmerge"      # pour mkvmerge -J (JSON)
MEDIAINFO_BIN="mediainfo"    # fallback (optionnel)

# Heuristiques & seuils
RUNTIME_TOLERANCE_SEC=120
EPISODE_GROUP_MIN=2
MAIN_FEATURE_MINUTES=60

# --- IA / LLM (OBLIGATOIRE) ---
LLM_ENABLE=1
LLM_PROVIDER="ollama"
LLM_ENDPOINT="http://127.0.0.1:11434"
LLM_MODEL="qwen2.5:14b-instruct-q4_K_M"
LLM_TEMPERATURE=0.2
LLM_TIMEOUT_SEC=3600
LLM_API_KEY=""
